aienv/
.env
__pycache__/

# NOTE: public/index.html is deliberately NOT ignored.
#
# It was, while this was a local-only tool. Cloudflare now serves public/
# straight out of this repository, so the built page IS the deployment - the
# daily GitHub Action commits it and that commit is what goes live.
